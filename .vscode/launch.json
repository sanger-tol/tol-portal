{
    "configurations": [
      {
        "name": "Python: Remote Attach",
        "type": "python",
        "request": "attach",
        "port": 15789,
        "flask": true,
        "host": "127.0.0.1",
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/tol-portal-api/app",
            "remoteRoot": "/app"
          },
          {
            "localRoot": "${command:extension.commandvariable.workspace.folder1Up}",
            "remoteRoot": "/packages/"
          }
        ]
      }
    ]
  }